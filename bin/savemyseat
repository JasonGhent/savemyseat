#!/usr/bin/env node
/**
 * savemyseat - A couchdb backup toolset
 */
var program = require('commander');

program
  .version('0.1.0')
  .command('initialize [database-config]')
  .command('monitor [database-config]')
  .parse(process.argv);
